# Host UID/GID the PHP containers run as, so files the app writes into the
# bind-mounted ./src stay owned by the host user. Set to your `id -u`/`id -g`.
PHPUID=1000
PHPGID=1001

MYSQL_DATABASE=laravel
MYSQL_USER=admin
MYSQL_ROOT_PASSWORD=secret
MYSQL_PASSWORD=secret